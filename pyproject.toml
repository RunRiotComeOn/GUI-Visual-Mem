[project]
name = "guimem"
version = "0.1.0"
description = "Task-specific and cross-task memory system for GUI-agent evaluation"
requires-python = ">=3.10"
dependencies = [
  "loguru",
  "numpy",
  "pillow",
  "pyyaml",
  "requests",
  "tqdm",
]

[tool.pytest.ini_options]
pythonpath = ["."]
